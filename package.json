{
  "name": "codex-harness",
  "version": "0.1.0",
  "description": "Codex-first programming harness CLI",
  "bin": {
    "ch": "bin/ch"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node scripts/run-acceptance.mjs",
    "test:acceptance": "node scripts/run-acceptance.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
